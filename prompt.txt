I want to create a python application, maybe using streamlit or gradio.

The app should be able to do the following:
- allow user to input text in two text areas
- compare the selected text and identify any differences in the words 
- highlight the differences in the text
- in the result, please maintain the orignal format of the text.
- allow user to download the highlighted text as a words document. Show the result side by side in 
words document.

Here is an additional feature that I want to add to the app:
- Create a SQLite database;
- User can input text, save, delete, update row records and column records in the database;
- Label the row as Wordings
- Label the column as Policies;
- User can select from any row records and column records for comparison.
- Output the database as a csv file, use Policies as the column names and Wordings as row name for each record;
- Give a button to export the database as a csv file.
